query GetBranchDetail($id: String!) {
  branch(id: $id) {
    id
    businessId
    name
    address
    coordinates {
      type
      coordinates
    }
    phone
    status
    avatarUrl
    coverUrl
    deliveryRadius
    acceptedCurrency
    exchangeRate
    createdAt
  }
}
