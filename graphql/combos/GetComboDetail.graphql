query GetComboDetail($comboId: String!) {
  combo(comboId: $comboId) {
    id
    branchId
    name
    description
    imageUrl
    currency
    availability
    discountType
    discountValue
    basePrice
    finalPrice
    savings
    representativeProducts {
      id
      name
      imageUrl
    }
    slots {
      id
      name
      description
      minSelections
      maxSelections
      isRequired
      displayOrder
      options {
        productId
        isDefault
        priceAdjustment
        product {
          id
          name
          imageUrl
          price
          currency
        }
        availableModifiers {
          name
          priceAdjustment
        }
      }
    }
    branch {
      id
      name
      avatarUrl
    }
    createdAt
  }
}
