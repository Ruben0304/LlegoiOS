query GetProductDetail($id: String!) {
  product(id: $id) {
    id
    branchId
    name
    description
    weight
    price
    currency
    imageUrl
    availability
    categoryId
    createdAt
    business {
      id
      name
    }
  }
}
