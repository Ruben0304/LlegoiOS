mutation LoginWithGoogle($input: SocialLoginInput!) {
  loginWithGoogle(input: $input) {
    accessToken
    tokenType
    user {
      id
      name
      email
      phone
      role
      createdAt
    }
  }
}
