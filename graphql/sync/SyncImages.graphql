query SyncImages($entityType: String, $entityIds: [String!], $qualities: [ImageQuality!], $since: DateTime) {
  syncImages(entityType: $entityType, entityIds: $entityIds, qualities: $qualities, since: $since) {
    entityId
    entityType
    imagePath
    urls {
      baja
      original
    }
  }
}
