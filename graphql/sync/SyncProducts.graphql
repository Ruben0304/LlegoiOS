query SyncProducts($availableOnly: Boolean, $since: DateTime) {
  syncProducts(availableOnly: $availableOnly, since: $since) {
    id
    branchId
    name
    description
    weight
    price
    currency
    image
    imageUrl
    availability
    categoryId
    createdAt
  }
}
