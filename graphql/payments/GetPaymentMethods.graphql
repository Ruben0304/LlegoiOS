query GetPaymentMethods($jwt: String, $branchId: String) {
  paymentMethods(jwt: $jwt, branchId: $branchId) {
    id
    name
    code
    currency
    method
    commissionPercent
    deliveryFeePercent
    isRefundable
    requiresProof
    requiresBusinessConfirmation
    expirationMinutes
    isActive
    displayOrder
    iconUrl
    instructions
  }
}
